# Image Classification API

A lightweight REST API built with FastAPI and TensorFlow, serving a pre-trained MobileNetV2 model for real-time image classification.

## Setup & Run

1. Install dependencies:
```bash
pip install -r requirements.txt